/** Size of the share-preview picture (1200 x 630 is what Facebook, X and WhatsApp recommend). Kept apart from share-image.ts, which needs the native image library and must not reach the browser. */
export const SHARE_WIDTH = 1200;
export const SHARE_HEIGHT = 630;
