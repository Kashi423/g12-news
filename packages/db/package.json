{
  "name": "@g12/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared Prisma schema and client for the web app and the worker.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "generate": "prisma generate",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "studio": "prisma studio",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@g12/config": "*",
    "@prisma/adapter-pg": "7.10.0",
    "@prisma/client": "7.10.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "dotenv": "^18",
    "prisma": "7.10.0",
    "typescript": "~5.9.3"
  }
}
